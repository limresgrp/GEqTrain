# @package _global_
defaults:
  - /base
  - /data: nmr
  - /model: nmr_interaction
  - /train: nmr
  - _self_

root: results/nmr
run_name: tensor_all_reduced
experiment_description: "NMR scalar and tensor chemical-shift tutorial."

log_batch_freq: 500
wandb: false
# wandb_project: nmr

# Optional: set this to an existing checkpoint to fine-tune instead of starting
# from scratch.
# fine_tune: /path/to/last_model.pth
