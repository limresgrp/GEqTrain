# @package _global_
defaults:
  - /base
  - /data: shiftml3
  - /model: shiftml3_interaction
  - /train: shiftml3
  - _self_

root: results/shiftml3
run_name: tensor_all_reduced
experiment_description: "ShiftML3 scalar and tensor chemical-shift tutorial."

log_batch_freq: 500
wandb: false
wandb_project: shiftml3

# Optional: set this to an existing checkpoint to fine-tune instead of starting
# from scratch.
# fine_tune: /path/to/last_model.pth
